Batch Ingest Completed But Had Errors
=======================++============

The batch submitted by <%= @batch.submitter_email %> has completed, but had errors.

To view its status, just follow this link:
<%= batch_url(@batch, host:  Rails.application.config.action_mailer.default_url_options[:host]) %>.

<% if @batch.error.present? %>
===================================
Batch Error:
<%= @batch.error %>
<% end %>

<% if @batch.batch_items.present? %>
===================================
Batch Items:
Total: <%= @batch.batch_items.count %>
Errors: <%= @batch.batch_items.select{ |bi| bi.error.present? }.count %>
<% end %>
